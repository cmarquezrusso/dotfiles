export PATH=$PATH:~/.asdf/installs/golang/1.20.6/packages/bin/
for file in ~/.{path,bash_prompt,exports,aliases,functions,extra,dockerapps,kubectl_aliases}; do
	[ -r "$file" ] && [ -f "$file" ] && source "$file";
done;